<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - Dev Vault</title>
    <link rel="stylesheet" href="{{ baseUrl }}/css/base.css">
    <link rel="stylesheet" href="{{ baseUrl }}/css/main_section.css">
    <link rel="stylesheet" href="{{ baseUrl }}/css/runbooks.css">
    <link rel="stylesheet" href="{{ baseUrl }}/css/homepage.css">
    <link rel="stylesheet" href="{{ baseUrl }}/css/navigation.css">
    <link rel="stylesheet" href="{{ baseUrl }}/css/filters.css">
    <link rel="stylesheet" href="{{ baseUrl }}/css/cards.css">
    <link rel="stylesheet" href="{{ baseUrl }}/css/responsive.css">
</head>

<body>
    <nav>
        <div class="nav-container">
            <a href="{{ baseUrl }}/" class="nav-brand">Dev Vault</a>
            <ul class="nav-links">
                <li><a href="{{ baseUrl }}/" {% if page.url=='/' %}class="active" {% endif %}>Home</a></li>
                <li><a href="{{ baseUrl }}/tools/" {% if page.url=='/tools/' %}class="active" {% endif %}>Tools</a></li>
                <li><a href="{{ baseUrl }}/bookmarks/" {% if page.url=='/bookmarks/' %}class="active" {% endif %}>Bookmarks</a></li>
                <li><a href="{{ baseUrl }}/runbooks/" {% if page.url=='/runbooks/' %}class="active" {% endif %}>Run Books</a></li>
            </ul>
        </div>
    </nav>

    <main>
        <div class="container">
            {{ content | safe }}
        </div>
    </main>
</body>

</html>
